#pragma once

// Easy visual and layout customization values.
namespace AppConfig
{
    inline constexpr int WindowWidth = 926;
    inline constexpr int WindowHeight = 582;
    inline constexpr float SidebarWidth = 73.0f;

    inline constexpr float CardCornerRadius = 4.0f;
    inline constexpr float ControlCornerRadius = 4.0f;
    inline constexpr float ScrollbarWidth = 4.0f;
}
